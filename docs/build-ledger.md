# CBAI Build Ledger

Grouped by series. Individual commit messages are the authoritative per-commit record
(`git log`); this ledger tracks series-level milestones so the shape of the work is legible
without reading 60+ commits.

## RI-BUILD series (001–033) — Research Intelligence catalog and UI foundation

Established the canonical `ResearchTopic` catalog, the topic detail page, the global research
network graph, workspace explorer, evidence navigation with URL-driven selection, and the
Research Review domain scaffolding (`lib/research/review/`). Consolidated a duplicate
`ResearchTopic` model (RI-BUILD-033B) after a naming collision was caught before it shipped.

## BUILD-004x series — Deterministic intelligence engines

| Build | Commit | What it added |
|---|---|---|
| 004A | `f50c5aa` | Evidence Gap Intelligence Engine — `lib/research/intelligence/` |
| 004B | `e0c8fa1` | Research Mission Workspace (mission, known info, unknowns, gaps) |
| 004C | `d61d53a` | Research Decision Engine — one deterministic recommendation |
| 004D | `8f2cb86` | URL-driven evidence selection (`?evidence=`), server-first |

## BUILD-005 — Review Operating System foundation (`5f1f54a`)

`TopicReviewWorkspace`, `ResearchReviewWorkspaceState`, honest empty states for notes/findings,
process-level open questions derived from real gap/review state.

## EPIC series — Platform-wide transformation (frozen at EPIC-10, RC-1)

| Epic | Commit | Scope |
|---|---|---|
| — | `40796cf` | Research Workspace professionalization (Context Bar, Mission Control Panel) |
| — | `d9533fb` | Research Readiness Engine — deterministic milestones, `lib/research/readiness/` |
| — | `bc77bea` | Research Health Engine — Healthy/Stable/Weak/Critical, `lib/research/health/` |
| — | `067b101` | Research Workflow Engine — stage, next action, real action links, `lib/research/workflow/` |
| — | `4cf366c` | Research Cockpit — one operational surface, consolidated Mission Control Panel + Timeline |
| EPIC-01 | `b8d65e3` | Universal product identity, positioning, and public entry experience |
| EPIC-02 | `a9f419c` | Universal Intelligence Foundation — `lib/foundation/`, research adapter |
| EPIC-03 | `05777e7` | Universal Relationship Engine — `lib/relationships/`, 16-type vocabulary, richer `Relationship` envelope; found and documented three pre-existing, unmigrated graph/relationship systems (`lib/graph/`, `lib/research/graph/`, `lib/intelligence/graph/`) |
| EPIC-04 | `0401f22` | Universal Evidence Operating System — `lib/evidence/` engine (build, validate, link, history, query/compare/trace); `Evidence` enriched to 21 fields; `EvidenceSourceType`/`VerificationStatus` promoted from existing Research/evidence-infrastructure modules (aliased, not duplicated); shared `Confidence` extracted for Relationship + Evidence; found and documented a third, non-integrated numeric-scoring Evidence model in `lib/intelligence/` |
| EPIC-05 | `32b8700` | Intelligence Reasoning Framework — `lib/foundation/reasoning-types.ts` (shape) + `lib/reasoning/` (engine): `buildReasoningResult` deterministically derives Observed Facts, Known Unknowns, Supporting/Conflicting Evidence, Reasoning Path, Possible Options, Trade-offs, Risks, Potential Consequences, Open Questions, and an always-`true` Human Decision Required flag from real Evidence/Relationship/Timeline input — never a model call; wired into `research-foundation-adapter.ts` (`IntelligenceFoundationView.reasoning`) and proven for all 65 catalog topics via `npm run build`; found and documented a fourth, non-integrated dormant subsystem (`lib/intelligence/engine/`, `orchestrator/`, numeric `ConfidenceAssessment`) |
| EPIC-06 | `0455def` | Universal Intelligence Workflow Framework — `lib/foundation/workflow-types.ts` (shape) + `lib/workflow/` (engine): `Workflow` connects Question/Mission/Evidence/Relationships/Reasoning/Execution to a 12-state, transition-audited state machine (`createWorkflow`, `applyWorkflowTransition`, `validateWorkflowTransition`, `validateWorkflowRecord`); every transition carries reason/timestamp/actor/evidenceReference (nullable, never fabricated)/previousState/nextState; wired into `research-foundation-adapter.ts` (`IntelligenceFoundationView.workflow`) and proven for all 65 catalog topics via `npm run build`; found and documented a fifth, non-integrated dormant subsystem (`lib/intelligence/agents/tasks/`, `lib/intelligence/runtime/`) |
| EPIC-07 | `bc3bb44` | Intelligence Orchestration Layer — `lib/foundation/orchestration-types.ts` (shape: `IntelligenceResult`, `pipelineTrace`, `IntelligenceExtensionPoints`) + `lib/orchestration/` (engine: `runIntelligencePipeline`, a plugin-based `IntelligencePipelineProviders` contract, `validateIntelligencePipelineProviders`); connects Foundation → Evidence Discovery → Relationship Resolution → Reasoning → Workflow into one pipeline with zero domain logic — Reasoning/Workflow consumed directly from `lib/reasoning/`/`lib/workflow/`, never re-derived; wired into `research-foundation-adapter.ts` (`runResearchIntelligencePipeline`), verified structurally via `npm run build`; found and documented a sixth, non-integrated dormant subsystem — a full parallel pipeline orchestrator in `lib/intelligence/orchestrator/` built on the same numeric scoring model flagged in EPIC-04/05 |
| EPIC-08 | `288884f` | Global Intelligence Network — `lib/foundation/network-types.ts` (shape: `IntelligenceEntityKind` 16-value vocabulary, `IntelligenceNetwork`, `CollaborationCandidate`, `NetworkExtensionPoints`) + `lib/network/` (engine: `buildIntelligenceNetwork`, `validateIntelligenceNetwork`, query primitives reusing `lib/relationships/`, `findCollaborationCandidates`); nodes are real Intelligence Entities (not a social graph — no followers, no messaging, no popularity), edges reuse the Foundation's `Relationship` type unmodified so every connection is evidence-aware/traceable by construction; every collaboration candidate traces to a real shared Evidence or relationship-target id, never a connection count; wired into a new `research-entity-network-adapter.ts` mapping the pre-existing `lib/research/entities/` catalog onto the network (resolving previously-documented technical debt), verified structurally via `npm run build` |
| EPIC-09 | `b3d8c21` | Intelligence Workspace Platform — `lib/foundation/workspace-types.ts` (shape: `WorkspaceView` and its nine sections — Mission Center, Intelligence Brief, Evidence Center, Knowledge Network, Recommendations, Monitoring, Timeline, Open Questions, Activity) + `lib/workspace/` (engine: `buildWorkspaceView`, `validateWorkspaceView`, boolean query readers); every section is a pass-through or trivial default over `IntelligenceResult` (EPIC-07) and `IntelligenceNetwork` (EPIC-08) — zero new intelligence logic, zero UI, zero components, zero routes; `extensions` reuses `IntelligenceExtensionPoints` directly rather than declaring a parallel vocabulary; wired into a new `research-workspace-adapter.ts` composing the EPIC-07/EPIC-08 pipelines with no new logic, verified structurally via `npm run build` |
| EPIC-10 | `298b834` | **CBAI Platform RC-1** — full Platform Core audit (Foundation through Workspace, 41 files/2,761 lines), Constitution audit, health analysis, and Core Freeze declaration. No new capability, no new domain, no new ecosystem. One real duplicate found and fixed: six identical `{valid, issues}` validator-result interfaces consolidated into `lib/foundation/validation-types.ts`'s `PlatformValidationResult` (zero-behavior-change alias, matching the `Confidence`/`EvidenceSourceType`/`VerificationStatus` promotion pattern). Zero circular dependencies confirmed by full import-graph audit. One doc-precision fix. One naming inconsistency (`createWorkflow` vs. `build*`) and six zero-caller validator exports identified and deliberately left as documented technical debt rather than risk-bearing renames/deletions. Zero Constitution violations found in the Platform Core across nine audited principles. Full audit trail: `docs/CBAI-Platform-RC1.md`. Platform Constitution amended with a ratified Platform Core Principles section (`docs/standards/01-cbai-constitution.md`) |

## Research Intelligence series — extensions built on the frozen Platform Core

Built after CBAI Platform RC-1 (EPIC-10). Each entry extends the Platform Core via the four
sanctioned Extension Points (`docs/CBAI-Platform-RC1.md`) — none modifies a frozen module.

| Build | Commit | Scope |
|---|---|---|
| Phase 1 | `95466c7` | Research Domain Foundation — `lib/research-domain/` (types only, no builders, no validators, no seed data): 27 Research entity interfaces (Research Mission through Research Impact), each extending `ResearchEntityBase`'s eight required concerns (Identity, Lifecycle, Relationships, Evidence Links, Mission Links, Organization Links, Timeline, Traceability), reusing `Evidence`/`Mission`/`Relationship`/`TimelineEvent`/`Question` from `lib/foundation/` directly rather than redeclaring them; a new, small, closed lifecycle vocabulary (`proposed \| active \| completed \| archived`) deliberately distinct from `WorkflowState`; `RESEARCH_RELATIONSHIP_PATTERNS` documents 30 realistic entity-pair connections using 15 of the Platform's existing 16 `RelationshipType` values — no new relationship vocabulary needed. Zero Platform Core files touched. |
| Phase 2 | `e1aa340` | Research Domain Integration — five new files in `lib/research-domain/`: `research-domain-builder.ts` (`buildResearchEntityBase`), `research-domain-adapter.ts` (real-data mapping from `lib/research/research-topics.ts`, `evidence/evidence-topic-builder.ts`, `intelligence/review-workspace-engine.ts`, `intelligence/workspace-shell-engine.ts`, `graph/`, `entities/`, plus `research-foundation-adapter.ts`'s existing `toSubject`/`toMission`/`toEvidence`/`toRelationships`), `research-domain-query.ts`, `research-domain-validation.ts` (reuses EPIC-10's `PlatformValidationResult`), and `research-domain-providers.ts` (`researchDomainPipelineProviders: IntelligencePipelineProviders`, EPIC-07's contract, unmodified). 65 real topics + missions + questions mapped, 1 real laboratory + 1 real dataset from the entities registry, Knowledge Graph `related_topic` edges converted to Relationships (uses_method/requires_evidence deliberately excluded as duplicates of `toRelationships()`; future_supports excluded as non-real). Researchers/Hypotheses honestly mapped to `[]` — no real data exists yet. Zero Platform Core files touched, zero Phase 1 files modified, zero legacy `lib/research/*` files modified. |
| Phase 3 | `9d8fe0c` | Research Workspace Contract — new `lib/research-workspace/` directory (five files, mirroring `lib/research-domain/`'s own separation from the Platform Core it sits on): `research-workspace-contract.ts` (`ResearchWorkspaceContract`, 19 sections — 8 are direct reuse/wraps of Platform's `WorkspaceView` sections, `ReasoningRisk`, and `CollaborationCandidate`; 11 are new thin compositions over Research Domain entities), `research-workspace-providers.ts` (`ResearchWorkspaceProviders`, mirroring `IntelligencePipelineProviders`'s injection-contract role), `research-workspace-builder.ts` (`buildResearchWorkspaceContract` — calls `buildWorkspaceView` (EPIC-09) for the reused sections, narrows a Research Domain entity collection by kind for the new ones, calls `findCollaborationCandidates` (EPIC-08) directly for Potential Collaborators), `research-workspace-query.ts`, `research-workspace-validation.ts` (reuses EPIC-10's `PlatformValidationResult`). Zero Platform Core files touched, zero Research Domain files modified, zero UI/React/components/pages. |
| Phase 4 | `e94b77b` | Research Mission Engine — new `lib/research-mission/` directory (five files, on top of Platform RC-1 + Research Domain (Phase 1/2) + Workspace Contract (Phase 3), all frozen and unmodified): `research-mission-engine.ts` (`MissionLifecycleState`, 9 states; `MISSION_STATE_TRANSITIONS`; `canTransitionMission`/`validateMissionTransition`/`applyMissionTransition`, mirroring `lib/workflow/workflow-transition.ts`'s (EPIC-06) proven shape for a new vocabulary; `ResearchMission`, `MissionTransition` (reuses `WorkflowActor`), `MissionMilestone`, `MissionDeliverable`), `research-mission-providers.ts` (`MissionProviders`, mirroring `ResearchWorkspaceProviders`'s (Phase 3) injection-contract role), `research-mission-builder.ts` (`createResearchMission`, `buildResearchMission` — every "Support:" concern is a direct reference into Workspace Contract/Research Domain output, only Goal/Scope/Milestones/Deliverables are genuinely new fields), `research-mission-query.ts`, `research-mission-validation.ts` (reuses EPIC-10's `PlatformValidationResult`). Zero Platform Core, Research Domain, or Workspace Contract files touched, zero UI/React/components/pages. |
| First Live Vertical Slice | `90f4e59` | Activated the first real UI consumer of everything built so far, for topic `microbiology`. Fixed two disconnected handoffs in `lib/research-workspace/` (added `RecommendedNextStep` — sourced from the existing `deriveResearchWorkflow` engine — to `MissionProgressSection`; added `intelligenceBrief` reuse of Platform's `IntelligenceBriefSection` to `MissionSummarySection`) and one latent timeline double-count in `buildResearchWorkspaceContract`. New server component `components/research/topic/ResearchIntelligenceOverview.tsx`, wired into the existing `/research/[topicId]` route alongside (not replacing) `ResearchTopicDetail`. New zero-dependency test harness (`npm run test:research-slice`, Node's built-in `node:test` + a small `@/` alias-resolving loader) — 10/10 tests pass. `npm run build` statically regenerates all 89 routes including all 65 real topic pages. Zero Platform RC-1, Research Domain, or Research Mission Engine files touched; two minimal, documented corrections to the already-shipped Workspace Contract (Phase 3). |
| Product Activation Audit | `dc3c31b`, `e1b0cb0`, `3a3cd5c`, `a78b2f3` | Full route/navigation/capability inventory (22 routes, 89 generated pages) plus the highest-value, safest fixes from the "CBAI Product Activation Program" master mission: sidebar now renders the six previously-hidden but fully working routes (`/dashboard`, `/reasoning`, `/government`, `/investor`, `/citizen`, `/ai-control`) as a "Workspaces" section; page metadata normalized under one root `title.template` across Home/Countries/Companies/Universities/Dashboard/Research routes (Countries/Companies/Universities `page.tsx` split into thin Server Component + sibling `*PageClient.tsx`, the documented Next.js pattern, since a Client Component cannot export `metadata`); one redundant `deriveResearchWorkflow` call removed from `/research/[topicId]` (lifted to the shared parent, passed as a prop to `ResearchCockpit` and `TopicReviewWorkspace`); re-verified the previously-flagged companies/universities fabricated-score issue and found it already remediated in a prior epic, but found and fixed one real residual inconsistency in `entity-evidence-mapper.ts` (country-only honest "withheld" framing generalized to any entity type via `hasAssessedScores()`). Full inventory and backlog: `docs/product-activation-audit.md`. Zero Platform Core, Research Domain, Workspace Contract, or Research Mission files touched. |
| Workspace Activation | `d4a8885` | Closed the one gap the vertical slice left open: `lib/research-mission/` (Phase 4) was built but never connected to the live UI. `ResearchIntelligenceOverview.tsx`'s single call site changed from `buildResearchWorkspaceContract` to `buildResearchMission` (which embeds the same Contract internally), adding a "Mission lifecycle" stat (honestly `draft` for every real topic — no mission has ever transitioned). One minimal correction: `BuildResearchMissionInput.goal`/`.scope` made optional, defaulting to real Research Domain text (`ResearchMissionEntity.statement`, `ResearchTopicEntity.description`) the function already resolves internally, so a UI caller needs only a subject id. `npm run test:research-slice` gained a new test (11/11 passing) confirming `buildResearchMission` and `buildResearchWorkspaceContract` share the same evidence set. Zero Platform RC-1, Research Domain, or Workspace Contract files touched; one minimal correction to Research Mission Engine (Phase 4). No new engine, domain, or contract. |
| Personal Intelligence Assistant (Release 2) | `984e730` | `lib/assistant/` — device-local Assistant profile (localStorage, honestly not a real account — no auth/backend exists in this static-export app) plus a deterministic phrase-to-route command table, zero new AI/reasoning engine. New persistent Command Center in `Topbar` (every route, never floating): keyboard input, real Web Speech API voice (feature-detected), honest upload-not-connected notice. `/settings` activated from a placeholder into a real Assistant profile form; accessibility toggles have real effect via document-level CSS. Signed-in-style home greeting shown only when a real local profile exists, honestly labeled where no real recommendation/activity data exists. Deleted a fully dead decorative "AI command center" mockup (`components/core/*`, `lib/core.ts`, zero references anywhere) rather than leaving a second, fake one beside the real Command Center. `npm run build` 91 routes, `npm run test:research-slice` 11/11 unchanged. Zero Platform Core files touched. |
| Empty States, Missing Capabilities, and Global Discovery (Release 3) | `f058fdb`, `0c8aa8d`, `cf6f775`, `b0380c0`, `fe725b6`, `684e841` | Repo-wide honest-empty-state audit found and fixed a real cluster of bare unexplained text (8 "Not yet" values across three `/research/review` panels, a bare dashboard empty state, a search card silently dropping its CTA with no visible cue). Activated `buildTopicResultEntry`/`SearchResultCard` — built for Global Search's knowledge/evidence/future-module result groups but never wired in, silently discarding real computed results every search — and added a genuinely new real "Research Topics" search group via the existing `filterResearchTopics` query (previously none of the 65 real research topics were findable by name through Global Search). Extended the Command Center's command table with new fixed commands and real parameterized patterns ("find country X," "show research topic X") resolved against real catalogs; unmatched input now shows an honest "not recognized yet" panel with supported alternatives instead of silently guessing search intent. New `lib/product-status.ts` + `StatusBadge` — one seven-value status vocabulary, visible label plus full-sentence explanation, never color-only — used in the fixed panels and a new `EntityDataStatus` component now on all three Country/Company/University panels. Activated `RecentEntities` a second time on `/my-work`. New `scripts/test-product-activation.ts`, 13 tests, alongside the unchanged 11 research-slice tests. Full detail: `docs/product-activation-audit.md` §8. Zero Platform Core, Research Domain, Workspace Contract, Research Mission, or Assistant architecture files touched. |
| Connected Intelligence Experience (Release 4) | `605fe76` | Investigated real cross-entity relationship data before building anything: found real Country↔Company↔University links already computed and already live (`get{Country,Company,University}Relationships`), just tucked inside a collapsed disclosure — surfaced a real count of them in a new panel rather than recomputing; found and deliberately left unwired a second, fully-computed, zero-caller relationship graph (`lib/registry/entity-links.ts`) that would have duplicated the first; confirmed research topics have zero real links to any country/company/university (Research Domain organization types are documented stubs). Activated a cluster of fully-built, zero-consumer country/company/university components: `*IndicatorCoverage.tsx` (real domain-grouped Economy/Judicial System/Education/Health/Research sections), `*SourceCoverage.tsx` (named official sources), and `CountryTimelineSection.tsx`'s timeline engine (country-only — no company/university timeline builder exists, so honestly none was added). New `components/shared/IntelligenceContextPanel.tsx` (real related-entity count, evidence, reports, open questions, status badge) replaces the narrower Release 3 `EntityDataStatus` (deleted, superseded). New `lib/assistant/assistant-context.ts` — the Assistant now derives current-page context (real research topic or focused platform entity) with zero new tracking and zero questions asked, shown as a Command Center context chip. 4 new tests (14–17) directly covering `resolveAssistantContext`. `npm run build` 91 routes, 28/28 tests passing (17 + 11). Full detail: `docs/product-activation-audit.md` §9. Zero Platform Core files touched; no new engines, AI systems, or duplicate workspaces. |
| Premium Global Interface & Personal Operator Experience (Release 5) | `e9ed9c7`, `2b6ae25`, `eca4676`, `4ca2b3a`, `5e6194b`, `23fb903` | "CBAI Personal Operator" is the product-facing name for the existing Assistant — copy only, no new AI, no second profile store; implementing it literally surfaced a real user-name/Operator-name gap in the Release 2 data model, fixed additively with one new `operatorName` field on the same `AssistantProfile` (backward-compatible). New `components/shared/Avatar.tsx` consolidates three separate inline avatar renderings into one component with a real accessible label and fallback state, now also used in the Command Center and a new Account Menu. Rebuilt the home arrival experience to one primary next step (new `lib/assistant/assistant-next-step.ts`, a pure deterministic resolver: continue real recent work, else open the role's real default workspace, else prompt setup) plus four named secondary actions, replacing a heavier four-tile grid. New `lib/world-map.ts` + `WorldIntelligenceMap.tsx` — a real, region-grouped, status-badged country grid with search, deliberately not a hand-coded geographic SVG (no map/geo-data library exists in this repo); placed on Home and `/countries` only. New `ContextualOperatorBanner.tsx` ("You are viewing {name}" + real entity-kind actions) wired into Countries/Companies/Universities/Research, reusing Release 4's `resolveAssistantContext` unchanged. 11 new tests (18–28). `npm run build` 91 routes, 39/39 tests passing (28 + 11). Full detail: `docs/product-activation-audit.md` §10. Zero Platform Core files touched. |
| Companies Intelligence Module Activation | `abcbec0`, `3cc3818`, `ecff7ce`, `813518e`, `b2a0db6`, `6690c5e` | Fixed a real, confirmed bug: bidirectional navigation between Country/Company/University "linked entity" lists was broken everywhere — real, correct names rendered as inert plain text, never a link. New `resolve-entity-link.ts` + `LinkedNamesList.tsx` fix this across `CompanyRelationships.tsx`/`CountryRelationships.tsx`/`UniversityRelationships.tsx`. Activated a fully-built, zero-caller bookmark system (`pinEntity`/`unpinEntity`) — a real "Save to workspace" button, a real "remove" action, and My Work's "Saved Work" section (previously claimed no persistence layer existed — it did, just unwired). New honest `lib/company-research.ts` — industry-keyword matching between companies and real research topics (labeled "related by subject matter," never a sponsorship claim), wired both directions. Added a real `website` field (real public URLs). Activated `CompanyMethodology.tsx`/`CompanyTrustSection.tsx` (dead, mission-requested); deleted redundant `CompanyCoveragePanel.tsx`. Evidence detail now honestly shows "Not assessed"/"Not available" for confidence/citation/publication-date rather than omitting them. New `lib/company-report.ts` compiles a real Company Report from already-computed data via a real "Generate report" button. Command Center gained `open company`/`compare companies`/`generate report`; `save workspace` is a real context-aware pin action. New `scripts/test-companies-intelligence.ts`, 15 tests. `npm run build` 91 routes, 54/54 tests passing (15 + 28 + 11). Full detail: `docs/product-activation-audit.md` §11. Zero Platform Core files touched. |
| Countries Intelligence Module Activation | `98e4476`, `ff1d053`, `e7459c3`, `73fe8ad` | Investigated Country↔Research first: confirmed no research topic references any country/region and `Country` has no industry-equivalent field to anchor a keyword match, so — unlike Company — no matcher was built; the required "Research topics" section states this honestly instead. Wired the confirmed-dead `SaveToWorkspaceButton` onto Countries. Added a real `officialWebsite` field (real government portal URLs for all 6 countries). Activated `CountryMethodology.tsx`/`CountryTrustSection.tsx` (dead, mission-requested); deleted redundant `CountryCoveragePanel.tsx`. `CountrySourceCoverage.tsx` now shows the same honest Publisher/Publication-date/Confidence/Citation fields Companies got. New `lib/country-report.ts` compiles a real Country Report (Overview, Evidence, Research, Organizations, Methodology, Trust statement, Limitations) via a real "Generate report" button. Command Center, global search, and bidirectional relationship links required no changes — already generic across entity kinds from prior missions. New `scripts/test-countries-intelligence.ts`, 12 tests. `npm run build` 91 routes, 66/66 tests passing (12 + 15 + 28 + 11). Full detail: `docs/product-activation-audit.md` §12. Zero Platform Core files touched. |
| Platform Relationship Activation | `6c25fd1`, `9336492`, `20928ce`, `ab8194d`, `efb5801` | Brought University to parity with Country/Company: activated dead `UniversityMethodology.tsx`/`UniversityTrustSection.tsx`, deleted redundant `UniversityCoveragePanel.tsx`, wired its first-ever `SaveToWorkspaceButton`, added a new `lib/university-report.ts` report, and enhanced `UniversitySourceCoverage.tsx` with the same honest evidence fields Country/Company have. University↔Research was honestly omitted (no signal exists, mirroring Country↔Research) rather than shown as an always-empty section. Extended the bookmark architecture (`pinEntity`/`EntityKind`) to Research topics — traced all 4 real switch consumers of `EntityKind` before adding a `research_topic` case to each; the pin functions themselves needed no change since they were already generic. A real `SaveToWorkspaceButton` is now on every research topic page. Fixed a real dead end: research topics' "Open evidence" action routed to a generic hub with zero Research awareness — repointed at a new real `id="evidence"` anchor on the topic's own already-real evidence section. New `lib/assistant/assistant-relationship-commands.ts` makes the Command Center relationship-aware — "open related research/company/university/evidence" and "open country" now resolve against whichever real entity is focused, using the same relationship functions every module already computes (one match navigates directly, several open the real listing, zero is an honest message). Browser-verified the full graph (Country → Company → Research → Company → Evidence → Save → My Work) for USA, Japan, and Germany — no dead ends, no fabricated links. New `scripts/test-relationship-activation.ts`, 14 tests. `npm run build` 91 routes, 80/80 tests passing (14 + 12 + 15 + 28 + 11). Full detail: `docs/product-activation-audit.md` §13. Zero Platform Core files touched. |
| Universal Entity Engine (Platform Core) | `cbd1c19`, `b02295c`, `72d0159`, `4c02e14`, `fe4e73a` | Extended the pre-existing `lib/entity/entity.types.ts` framework (previously search-only) rather than building a second one: `Entity` gained optional `summary`/`country`/`organization`/`relationships`/`reportsAvailable` fields (additive, every existing caller untouched); `EntityType` gained `research_topic` with a new `lib/research-topic.adapter.ts`. New `lib/entity/entity-relationships.ts` (`buildEntityRelationships`) normalizes the already-real per-module relationship functions onto one shared vocabulary — not a new engine. New `components/shared/EntityRelatedPanel.tsx` consumes it; migrated `ResearchRelatedCompanies.tsx` onto it with verified byte-identical output. New `lib/entity/entity-report.ts` (`buildEntityReport`) is a dispatch facade proven byte-identical to the direct `buildCountryReport`/`buildCompanyReport`/`buildUniversityReport` calls, plus a new minimal honest report for Research topics. Confirmed Workspace pin architecture and the main Sidebar were already fully universal. Migrated the Command Center's relationship resolver onto `buildEntityRelationships`, verified against the prior mission's 14 tests before adding new ones. Built `components/shared/EntityHeader.tsx` (one header for any entity type) but did not insert it into any live page, since every existing page's header already works and swapping would risk a visual regression or lose bespoke facts — recorded honestly as built-and-tested, not yet live. Universal Search was not migrated (real duplicate-result regression risk); `toResearchTopicEntity` is ready for that migration later. New `scripts/test-universal-entity-engine.ts`, 13 tests. `npm run build` 91 routes, 93/93 tests passing (13 + 14 + 12 + 15 + 28 + 11). Full detail: `docs/product-activation-audit.md` §14. Zero Platform Core files touched; no rewrite, no routing changes, no visual regressions. |
| Platform Core Completion | `3d17b52`, `8427e7c`, `9dcd234`, `5cdc364`, `8e4e4c8`, `14a3ec7`, `befc77b` | Finished every §14 deferral. `EntityRelatedPanel` now replaces `CountryRelationships.tsx`/`CompanyRelationships.tsx`/`UniversityRelationships.tsx`'s duplicate rendering — resolved the regression risk by tracing the Knowledge Graph builder to its source (built from the same functions the name-list relationships were, so it's a strict superset) and adding real `label`/`verified` fields to `EntityRelationship` so nothing was lost. `EntityHeader` migrated into Country/Company/University (same props, zero output change) and, newly, Research — replacing `ResearchTopicHero.tsx`'s generic boilerplate with real per-topic data while preserving its real safety statement. Research topics gained a fourth real report (`ResearchTopicReportView.tsx`); Country/Company/University's report buttons now call the shared `buildEntityReport` facade instead of their direct builders. Every report now carries a real `dataStatus` (`StatusBadge`), completing Trust's Methodology/Trust/Limitations/Data Status through the shared Entity layer. Universal Search completed: research topics are real `Entity` objects in the unified `searchEntities()` index, rendered through the same result card as Country/Company/University — the bespoke research-topic-only search path was removed outright, after closing a real recall gap (real tags from methods/evidence-types) so nothing was lost. Command Center's triplicated inline focus-resolution chain was replaced with the existing `getPrimaryEntity(context)` accessor. New `scripts/test-platform-core-completion.ts`, 10 tests. `npm run build` 91 routes, 103/103 tests passing (10 + 13 + 14 + 12 + 15 + 28 + 11). Full detail: `docs/product-activation-audit.md` §15. Zero Platform Core files touched; no working functionality removed. |
| Research Workspace Activation | `cbbd679`, `0060fae`, `a877937`, `562bdd3`, `6ea1059`, `4869878` | Investigated first: `/research/workspace` is a genuinely separate, honestly-labeled read-only topic switcher, not the same system as `lib/research-workspace/`'s real Contract — the real workspace already lived on the topic-detail route, where `TopicReviewWorkspace.tsx` already rendered "Research notes"/"Findings"/"Open review questions" headings, always empty because no persistence existed. New `lib/research/research-workspace-store.ts` — real localStorage Notes/Findings persistence, following the exact `context-history.ts` pattern; every note optionally links to a real Evidence item or related Entity. New Evidence Lifecycle (Collected → Reviewed → Linked → Compared → Referenced → Included in Report → Archived) — confirmed no existing status vocabulary matched this, applied over real catalog evidence, advancing one stage at a time, never skipped. New `SupportingCounterEvidencePanel.tsx` surfaces the real, previously-dropped `evidenceSummary.conflictingEvidence`/`.supportingEvidence` with equal weight. New `ResearchWorkspaceDashboard.tsx` composes Question/Progress/Evidence Summary/Missing Evidence/Recent Notes/Related Reports/Workspace Status from already-computed real sources — zero new engines. New `ResearchWorkspaceActivity.tsx`, a real single-user activity feed. `ResearchTopicReport` gained real `question`/`supportingEvidence`/`counterEvidence`/`notes` fields. New `scripts/test-research-workspace-activation.ts`, 12 tests. `npm run build` 91 routes, 115/115 tests passing (12 + 10 + 13 + 14 + 12 + 15 + 28 + 11). Full detail: `docs/product-activation-audit.md` §16. Zero Platform Core files touched; `/research/workspace`'s separate shell left unchanged. |
| Project Engine Activation | `3c36fbb`, `a4951e8`, `1a90495`, `709d73d`, `cc6cace`, `8d9a9d5`, `6ff2c13` | Project becomes the fifth `EntityType` — `toProjectEntity` participates in Universal Search, the Relationship Engine (`buildEntityRelationships("project", id)`, both directions, every other entity kind gained a real backlink), and the Report Engine (`buildEntityReport("project", id)`). 8 configuration-only Project Types (no code branches on a type id). New `lib/project/project-store.ts` — real localStorage persistence for Projects, Notes, Tasks, Open Questions, Evidence references, following the exact `context-history.ts` pattern. No new page: Project Home lives inside the existing `/my-work` route as a query-param view (`/my-work?project=id`), the same pattern Country/Company/University already use — `MyWork.tsx` restructured into a Suspense-wrapped `useSearchParams()` reader, Project-first (Recent/Pinned Projects above the pre-existing sections, none removed). Project Progress is six real boolean checks, mirroring Research Progress's milestone pattern — never a fabricated percentage. Command Center gained `create project`/`open project` (fixed) and `continue project`/`add evidence`/`open notes` (real most-recently-updated project — deliberately avoided adding `useSearchParams()` to the global Command Center, which would force a Suspense boundary everywhere). Search gained a real "Create Project from this entity" action on every profile page and openable search result card. New `scripts/test-project-engine.ts`, 15 tests. `npm run build` 91 routes, 130/130 tests passing (15 + 12 + 10 + 13 + 14 + 12 + 15 + 28 + 11). Full detail: `docs/product-activation-audit.md` §17. Zero Platform Core files touched; no new pages; nothing removed. |
| Intelligence Guide Activation | `8590959`, `44eed62`, `18ee7ce`, `2be0677` | The Assistant becomes an Intelligence Guide — a pure function over real Project state, never a second chatbot or model call. New `lib/project/project-guide.ts` (`resolveProjectGuideStep`) checks the mission's exact order (Question → Objectives → Evidence → Related Entities → Notes → Report) and returns one real suggestion, phrased only as "Suggested Next Step"/"Continue"/"Available Action"/"Ready When You Are" — never "must"/"required"/"mandatory." `Project` gained a real persisted `reportGeneratedAt` field (`markProjectReportGenerated`), replacing the prior mission's ephemeral session-only report flag — `deriveProjectProgress`'s report milestone, the Report Engine's timeline ("Report generated" event), and the new Project Health panel all now read this one honest, cross-session source. New `lib/project/project-health.ts` (`deriveProjectHealth`) surfaces eight real signals (question/objectives/evidence/notes/entity links/report/tasks/open questions) as booleans and counts — deliberately never a score or a percentage. New `ProjectGuidePanel`, `ProjectHealthPanel`, `ProjectTimelinePanel` (the last reusing the Report Engine's already-real timeline, not a new one) mounted on Project Home; fixed a real, previously-dead wiring gap where `ContextualOperatorBanner` was never mounted on `/my-work` and never received a `projectId`, so its "project" action case had been unreachable since it was built in the Project Engine Activation mission — now mounted with a real `projectId` prop and a live "Open Next Step" chip. Every "no data yet" empty state across Evidence/Notes/Tasks was rewritten to teach ("No Evidence has been added yet. Start by collecting one verified source."). My Work project cards now show real status, the real Guide suggestion, real last-activity timestamp, and a Continue button that deep-links straight to the suggested step's anchor. Command Center gained `open next step`/`generate project report`/`open project evidence`, deliberately distinct phrasing from the relationship resolver's existing bare `open evidence` (which already owns that phrase for whichever entity is focused) so nothing was silently shadowed. Fixed a real cross-panel staleness bug: Dashboard/Guide/Health/Timeline now read directly from the store on every render instead of caching via `useState` initializers, and mutating panels call a shared refresh callback, so the Guide's suggestion always reflects the current session's real actions, not just the last full page load. New `scripts/test-intelligence-guide.ts`, 12 tests. `npm run build` 91 routes, 142/142 tests passing (12 + 15 + 12 + 10 + 13 + 14 + 12 + 15 + 28 + 11). Full detail: `docs/product-activation-audit.md` §18. Zero Platform Core files touched; no new pages, no new chatbot, no fabricated recommendations. |
| Trust & Production Polish (EPIC 1) | `a9379d8`, `774668e`, `657ac3c`, `425554c`, `6b32a7f` | Response to a 7-persona browser audit's launch blockers — zero new features, zero layout changes. New `SystemPageShell` backs a branded root `not-found.tsx`, `(dashboard)/error.tsx`, `error.tsx`, and a dependency-free `global-error.tsx` — Return Home/Go Back/Search/Continue Project/Feedback, verified against the real production `out/404.html`. `PLATFORM_BUILD`/`PLATFORM_EVOLUTION_PHASE` deleted outright (the literal internal string they produced was visible on the Trust page and Home footer); Trust Center restructured around Methodology/Verification Model/Evidence Policy/Data Sources/Known Limitations/Transparency Statement. "Agents" removed from primary navigation (self-disclosed non-functional after the click); `/core`/`/workflows` confirmed already unreachable from any real nav. Engineering words (Runtime/Pipeline/Architecture) replaced with production language on every live page, including all 65 Research Topic pages. Five recoverable states (entity not found, project missing, search empty, relationship missing) rewritten to explain what happened/why/what's next. New `scripts/test-production-readiness.ts`, 15 tests. `npm run build` 91 routes, 157/157 tests passing (15 + 12 + 15 + 12 + 10 + 13 + 14 + 12 + 15 + 28 + 11). Full detail: `docs/product-activation-audit.md` §19. |
| Data Activation Layer (EPIC 2) | `d9e5053`, `37a96ad`, `07a435c`, `88ee6b9` | Maximized real connected information from the existing local catalog only — no fabrication, no scraping. Investigated every "obvious" relationship the mission named and confirmed Company↔Country/University↔Country/Company↔Research↔Company were already fully connected by prior missions' Knowledge Graph work (190 real relationships, verified by count). Country↔Research/University↔Research confirmed to have no real connecting field anywhere in the catalog — rather than invent a cross-taxonomy mapping, University gained the same honest "not connected" statement Country already had (`UniversityRelatedResearch.tsx`). Biggest real find: `INDICATOR_DOMAIN_CATALOG`'s real `futureExpansion` field (22 domain-specific sentences) existed since the indicator framework was built but was never rendered — new `EntityFutureSources.tsx` surfaces it as "Expected Future Sources" on every Country/Company/University profile and report. Reports gained real per-source `connectedSourceNames`/`missingSourceNames` breakdowns rendered as separated "Connected Evidence"/"Missing Evidence" lists (previously only an aggregate count). Search results gained a real "X of Y sources connected" `coverageLabel`, replacing a hardcoded "Available now" shown for every result regardless of real coverage. Compare pages fixed generic "First profile"/"Second profile" labels (real entity names) and a misleading "Shared sources" metric (relabeled "Shared source references" with an explicit non-connected-evidence caption). Real counts: 6 countries/8 companies/8 universities/64 research topics; every entity has exactly 1 of its real sources connected (22 of 182 total) — unchanged by this mission, since no new source was fabricated; the real change is visibility of already-real, previously-hidden data. New `scripts/test-data-activation.ts`, 14 tests. `npm run build` 91 routes, 171/171 tests passing (14 + 15 + 12 + 15 + 12 + 10 + 13 + 14 + 12 + 15 + 28 + 11). Full detail: `docs/product-activation-audit.md` §20. |
| Authentication + User Platform Foundation | `ecca26a`, `1d57b8b`, `accbc3f`, `7b5619f` | Real local accounts (salted SHA-256 via Web Crypto, real sign-up/sign-in/sign-out, a real `AuthProvider` using the same `useSyncExternalStore` pattern `AssistantProfileProvider` established) honestly labeled as device-local, not server-verified — a static export has no server to verify against. Confronted "prepare the platform for Supabase" honestly: real cloud storage is inherently async while every existing store in this app is synchronous, so converting the whole app would be the redesign this mission rules out, and there is no real Supabase project in this environment anyway — built a real, typed `CloudStorageAdapter` interface and a `SupabaseStorageAdapter` that honestly rejects every call as unconfigured instead of faking a connection (`lib/storage/storage-provider.ts`). Every Project, Bookmark, and Recent-Activity entry now belongs to the real signed-in user via a real per-user key namespace (`lib/storage/namespaced-key.ts`) — the existing `project-store.ts`/`context-history.ts` read/write helpers needed zero call-site changes, and a one-time migration preserves every existing user's pre-auth data under a shared `:local` bucket. New `/account` page and real-auth-aware `AccountMenu`/`MyWork` (corrected the now-false "CBAI does not yet have accounts" copy). New `scripts/test-auth-platform.ts`, 12 tests. `npm run build` 92 routes (`/account` is the one new page), 183/183 tests passing (12 + 14 + 15 + 12 + 15 + 12 + 10 + 13 + 14 + 12 + 15 + 28 + 11). Full detail: `docs/product-activation-audit.md` §21. Not attempted: real cloud persistence, email verification/password reset/rate limiting, per-account Assistant profile namespacing, Saved Reports ownership (no report persistence exists at all yet), multi-tenant Organization model. |
| Maximum Launch-Blocker Remediation | `1dfd90c`, `e786d8e`, `791f6f9`, `cd2f8fc`, `923300b`, `2f24df7`, `a18b009` | Repair mission — reused every existing engine, zero new architecture. Found and fixed a real, verified P0: `OfflineBanner` reported "offline" on every server render (dev and the real production static export) because modern Node's global `navigator.onLine` is always `undefined` — confirmed via curl against actual `out/*.html`, fixed with a real `typeof window` check. Removed a real report-copy contradiction ("does not open a report" beside a working "Generate report"). Activated real report output: generation timestamps and a real print/export action (`window.print()`, honestly satisfying "PDF export" via the OS print dialog, no new dependency) across all 5 report types; Project reports gained Objectives/Tasks/Open Questions that were already computed but never rendered. Found and fixed a real Trust Center accuracy regression (Privacy/Known Limitations still claimed "no user accounts," false since the Authentication mission); added a real, honest Terms of Use section. Government/Investor/Citizen each gained a real "Start a [Role] Project" entry point reusing the Project Engine directly, mapped to already-existing Project Types. `CreateProjectForm` now disables unavailable Visibility options and shows real Project Type descriptions; Project Home distinguishes Linked Entities from Bookmarks with a real toggle and shows a real save confirmation for Research Question/Objectives; Countries no longer forces the World Map open when a country is already selected. 11 previously-unspecified dashboard pages gained explicit metadata (one canonical product identity); Agents/Core/Workflows marked noindex. New `scripts/test-launch-gate.ts`, 17 tests. `npm run build` 92 routes, 200/200 tests passing (17 + 12 + 14 + 15 + 12 + 15 + 12 + 10 + 13 + 14 + 12 + 15 + 28 + 11). Full detail: `docs/product-activation-audit.md` §22. |
| Real Supabase Authentication + Cloud Persistence | `42d8b65`, `3cce006`, `cdccf1e`, `72915c9`, `274b39c`, `3bab8d4` | Converts the device-local-only account system into a real, optional cloud platform. Installed `@supabase/supabase-js` (browser-only — this static export has no server to hold a service-role key). Real Supabase Auth (sign-up/in/out, session restore, forgot/reset password via new `/reset-password` route, email confirmation); `AuthProvider` exposes `accountMode: "cloud" \| "device-local" \| "signed-out"`. Real production schema (`supabase/migrations/`) — UUID keys, typed relational tables (never a JSON blob), RLS on all 11 tables with exactly 4 own-record policies each, statically verified by 2 new tests reading the real SQL. Rather than converting the app's synchronous localStorage stores to unsafe async behavior, local storage stays the single source of truth for every reader; a new localStorage-persisted outbox (`lib/supabase/outbox.ts`) queues real background cloud writes with retry/backoff and visible Saving/Saved/Could-not-save status. Local-to-cloud migration and cloud-to-local pull-sync are both idempotent via a `(owner_id, local_id)` dedup key. Reconciled the prior mission's KV-shaped `CloudStorageAdapter` with the new relational schema rather than duplicating it. Found and fixed two real gaps: no report type had ever been persisted anywhere (new `lib/reports/reports-store.ts`, real "Save to My Reports" ownership across all 5 report types) and the Assistant profile was one flat key shared by every account on a browser (now real-identity-namespaced and cloud-synced, with an explicit-consent import prompt on divergence). Trust Center corrected again (no longer claims "no server"/"no cross-device sync"), gained a real Account Deletion statement. New `scripts/test-cloud-platform.ts`, 22 tests. `npm run build` 92 routes (`/reset-password` is the one new route), 222/222 tests passing. Not browser-verified against a live Supabase project (none exists in this environment). Full detail: `docs/product-activation-audit.md` §23. |
| Premium Interface + Global Language Foundation + Multilingual Voice Commands | `6648b2b`, `fddccd8`, `723e15b`, `f105424`, `76bc0a3`, `c3dc1d1` | Migrates the first screen toward the approved concept (no mockup image was present in this conversation; Phase 17's written palette spec served as the real reference) and activates a real 4-language interface (English/Uzbek/Russian/Turkish) plus multilingual voice/text commands — reused every existing engine, no second Assistant/Command Center/Project Engine. Real i18n foundation: one compile-time-checked `TranslationDictionary` shape; `useTranslation()` reads the existing `AssistantProfile.preferredLanguage` (inherits its persistence, no second state system); HTML `lang`/`dir` sync post-hydration. Found and fixed a real bug via the new test suite before shipping: multilingual "open country" commands checked only the English catalog name, so the mission's own worked Uzbek example never resolved — added real localized country names (`lib/i18n/country-names.ts`) for all 6 catalog countries. Extended the existing command resolvers with real Uzbek/Russian/Turkish phrases; added a real "change language" command; gave `AssistantCommandCenter` a real voice state machine (idle/requesting/listening/processing/permission-denied/network-error) from actual `SpeechRecognition` events, plus a `size="prominent"` variant (via `useId()`) for the new home-page command bar. Home page assembled from real pieces: prominent command bar, 11 Role/Work-Context cards (3 new `WorkspaceRole` values, no new data model), a Projects panel wrapping the existing `ProjectList`, an Intelligence Feed reading only real Project activity and saved Reports (honest empty state otherwise). Navigation simplified to the approved 7-item structure; real mobile nav drawer added (none existed before). Brand refreshed (not replaced): "UNIVERSAL INTELLIGENCE" subtitle, light/dark logo variant, new `app/icon.svg`, print-visible report headers. Found and fixed a real 404 (`/reports`, named in the mission's own checklist) by adding it as a real alias for `/analytics`. New `scripts/test-global-interface.ts`, 27 tests. `npm run build` 92 routes (`/reports` is the one new route), 249/249 tests passing. Not browser-verified: live voice recognition against a real microphone/browser. Full detail: `docs/product-activation-audit.md` §24. |

## Pattern established across every engine build

1. Inspect the real, existing data model before writing anything.
2. New engine consumes the engine directly below it — never re-derives.
3. Every "not yet possible" state renders an honest empty state or non-interactive requirement,
   never fabricated content.
4. `npm run lint && npm run build` before every commit; `git diff --stat` reviewed to confirm
   the change is scoped to what was intended.
