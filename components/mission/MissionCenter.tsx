"use client";

import IntelligenceCanvas from "@/components/canvas/IntelligenceCanvas";

/**
 * Mission Center — Intelligence Canvas operating environment (BUILD-013).
 * Preserves Mission Center route; canvas replaces stacked card layout.
 */
export default function MissionCenter() {
  return <IntelligenceCanvas />;
}
