/**
 * Lightweight test harness metadata for dashboard and tooling (BUILD-061).
 *
 * Keep scenario count in sync with INTELLIGENCE_TEST_SCENARIOS length.
 */
export const INTELLIGENCE_TEST_HARNESS_VERSION = "0.1.0-test-harness";

/** Built-in scenario catalog count — update when adding harness scenarios. */
export const INTELLIGENCE_TEST_SCENARIO_COUNT = 34;
