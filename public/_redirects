# Cloudflare Pages redirects — CBAI static export
# https://developers.cloudflare.com/pages/configuration/redirects/
#
# Static assets (e.g. dashboard.html) are matched before these rules apply.

# Root redirect — matches app/page.tsx behavior
/ /dashboard 302

# SPA-style fallback — unmatched routes serve Next.js 404 page
/* /404.html 404
